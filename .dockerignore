# Dependencies — reinstalled inside the image
node_modules

# Build output
dist

# Git
.git
.gitignore

# Env files — inject at runtime, never bake into the image
.env
.env.*
!.env.example

# Editor & OS
.DS_Store
*.pem
.vscode
.idea

# Redis dump (local dev artifact)
dump.rdb

# Logs
*.log
